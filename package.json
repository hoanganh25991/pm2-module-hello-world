{
  "name": "pm2-module-hello-world",
  "version": "0.0.1",
  "description": "Appache Request",
  "author": "Anh Le Hoang",
  "license": "MIT",
  "dependencies": {
    "babel-preset-node7": "^1.5.0",
    "pmx": "beta"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^4.0.0",
    "npm-run-all": "^4.1.2",
    "prettier": "^1.4.4"
  },
  "scripts": {
    "precommit": "lint-staged",
    "build": "babel src --out-dir=dist"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write --no-semi --print-width 120",
      "git add"
    ]
  },
  "config": {
    "conf_var_1": true,
    "conf_var_2": "myvalue"
  },
  "apps": [
    {
      "merge_logs": true,
      "max_memory_restart": "200M",
      "script": "index.js"
    }
  ]
}
